<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Drift data may be restored for explicit-login reconciliation. -->
    <include domain="database" path="." />

    <!-- With an include allowlist, all other domains are implicitly excluded. -->
</full-backup-content>
